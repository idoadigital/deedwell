{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs only. No shell, fs, or process access — the app is a pure HTTP client of the Deedwell API.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
