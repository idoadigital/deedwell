[package]
name = "deedwell-desktop"
version = "0.1.0"
description = "Deedwell — AI workforce workspace for nonprofit organizations"
edition = "2021"
license = "UNLICENSED"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[profile.release]
codegen-units = 1
lto = true
strip = true
